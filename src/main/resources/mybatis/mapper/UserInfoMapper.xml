<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.LiteTravel.web.mapper.UserInfoMapper">
    <insert id="insertInfo" parameterType="UserInfo">
        INSERT INTO travel_user_info(user_id, user_name, user_avatarUri)
        VALUES (#{userId}, #{username}, '/avatar.jpg');
    </insert>
    <update id="updateUserInfo">
        UPDATE travel_user_info
        SET user_name      = #{userName},
            user_gender    = #{userGender},
            user_birth     = #{userBirth},
            user_phone     = #{userPhone},
            user_email     = #{userEmail},
            user_address   = #{userAddress},
            user_avatarUri = #{userAvatarUri}
        WHERE user_id = #{userId};
    </update>
    <select id="findInfoById" resultType="UserInfo">
        SELECT *
        FROM  travel_user_info
        WHERE user_id = #{userId};
    </select>
</mapper>