<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travelplatform.web.mapper.UserMapper">
    <insert id="insertUser" parameterType="string">
        INSERT INTO sqlconnect.sys_user(user_code, user_name, user_password, user_state)
        VALUES (#{userCode}, #{userName}, #{userPassword}, '1')
    </insert>
    <select id="findUser" parameterType="string" resultType="user">
        SELECT *
        FROM sqlconnect.sys_user
        WHERE user_code = #{userCode}
          AND user_password = #{userPassword}
          AND user_state = '1'
    </select>
    <select id="findUserByCode" parameterType="string" resultType="user">
        SELECT *
        FROM sqlconnect.sys_user
        WHERE user_code = #{userCode}
    </select>
</mapper>